```python
def game_of_stones(n):
    if n % 2 == 1:
        return "Alice"
    else:
        return "Bob"

n = int(input())
print(game_of_stones(n))
```

This code addresses the errors in the previous version:

- Corrected the spelling of "start_of_turn" to "start_of_turn".
- Corrected the missing closing parenthesis for the input function.
- Corrected the missing closing quotation mark for the output.